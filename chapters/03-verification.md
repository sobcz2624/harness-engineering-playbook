# 闭合回路：自动化质量保障

上一章建立了规约体系。你有了清晰的验收标准、机器可读的规约结构、信任环和维护机制。但规约本身只是一份契约。没有验证的规约，就是一份愿望清单。

验证是 Agent 驱动开发中的控制系统。它在三个层面发挥作用：作为起点的锚，将规约变为可执行的约束；作为过程中的护栏，在 Agent 执行的每一步提供即时反馈；作为终点的闸门，在代码合并前做最终判定。

有了规约加上验证，单任务的闭环就合上了。你可以可靠地完成一个功能从 spec 到代码到合并的全流程。这已经足够支撑一个团队的日常迭代开发。
