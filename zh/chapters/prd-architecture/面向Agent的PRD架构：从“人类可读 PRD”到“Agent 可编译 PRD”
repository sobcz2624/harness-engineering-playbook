第X章｜面向Agent的PRD架构：从“人类可读 PRD”到“Agent 可编译 PRD”


在传统软件工程里，我们习惯一句话：代码主要写给人看，机器只是顺便执行。核心问题是“代码如何对人优雅”。
但在 Agent 时代，这句话需要改写。

对 Claude Code、Codex 这类大模型 Agent 来说，代码和文档本质上没有区别。
对人来说，代码是实现。
对 Agent 来说，代码和文档本质上是同一件事的两种形态：
文档是高层规格，代码是规格编译后的可执行结果。
真正决定交付质量的，不再只是代码风格，而是 PRD 是否能被 Agent 稳定地拆解、理解、执行、验证。
核心问题变成“规格如何对 Agent 优雅”。

这就是一个关键转向：
我们不再只追求人类视角下的优雅与可维护，而要建立 Agent 视角下的优雅与可维护。
所以，PRD需要架构
没有 PRD 架构，Agent 只能一次次做大跨度猜测；
有 PRD 架构，Agent 才能在红-绿-重构循环里稳定地产出。

———

代码工程早已有成熟方法：事件驱动架构、MVC、单例、工厂模式。
这些方法解决的是同一个问题：边界清晰、职责单一、低耦合高内聚。

但 PRD 领域长期缺一件事：PRD 的架构。
过去 PRD 主要服务“人读人写”，现在它必须服务“人定方向、Agent 执行实现”。

所以，面向 Agent 的 PRD 不应是一篇大而全的说明文，而应是一套可递归分解的结构化系统：
从业务意图出发，逐级收敛到原子任务；每个任务都自包含、边界清晰、跨度可控，能够直接进入 Agentic Loop（红-绿-重构）。

这套方法可以概括为一句话：

PRD = 一棵领域树，领域树负责“由总到分”：把业务目标拆成完备且正交的子域，再落到可执行任务。
注：文章中的理念，可直接使用skill prd-domain-tree-prover 运行，skill地址
https://github.com/stock-programmer/xuwu-skills/tree/main/prd-domain-tree-prover

———

一、Agent 时代的 PRD 架构目标

一个合格的 Agent-Ready PRD，不再只追求“读起来顺”，而要满足四个工程目标：

1. 边界清晰：每个子域只负责一类核心业务逻辑
2. 低耦合：跨域只走契约（API/Event），不共享可写内部模型。
3. 可验证：能证明完备性与正交性，而不是“看起来差不多”。
4. 可编译：最终能落到原子、自包含、粒度合适的任务单元。

———

二、把 PRD 当作“可编译输入”的分解流程

用一棵“Domain Tree（领域树）”来承接 PRD，从总到分、从计划到执行。

先判断当前节点“必须具备什么能力、禁止出现什么能力”，并为每项能力找到证据。证据不足就不继续拆

拆分要具备三大约束验证
    C1 完备性：每个事实都被覆盖且只被一个域覆盖。
    C2 正交性：状态单写入、事件单生产、无共享写模型。
    C3 最小性：删除任一域会丢能力；任意两域合并会触犯硬约束（不变量、生命周期、审计、隔离、权限边界）。

叶子域落地
    只有满足“可独立实现、可独立权限、契约完整、再拆会破坏一致性”的节点，才算叶子域。
    叶子域直接生成实现规格：实体、状态机、命令、事件、API、存储、测试。


四、以一个MES 示例进行说明：四个完备且正交的子域

需求：
    帮我做一个工厂生产的MES信息系统啊，专门用于追踪工单和物料还有工序工艺流程的一个服务。
    当前在工单生产过程中，我需要进行物料绑定。物料会有对应的BOM和BOM版本，BOM当中会存储在当前生产物料中所需要的所有的生产
    原料，并且工单中可以去绑定对应的工艺流程和工序信息。工艺流程对应多个工序工艺步骤，每个工艺步骤需要有自己的处理方式，
    可以绑定人员或者是设备编码。工单的生产，语言严格按照工艺步骤进行生产执行。需要考虑工艺步骤的顺序或者并行执行。
    工单中要有对应物料的生产批次数量追踪，在每一个工艺步骤上要有批次数量，良品和不良品数量追踪，
    对应的每一个工单得工艺步骤都需要记录对应的过站信息方便进行生产数据追踪。

你的 MES 需求可稳定落成四个一级子域：

1. 工单生命周期与 BOM/工艺绑定快照
    负责工单状态推进、绑定关系、绑定时点与责任人快照。
2. 物料主数据、BOM 与 BOM 版本发布
    负责“做什么料、用什么料、版本是什么”的主数据与版本治理。
3. 工艺路线、工序步骤、步骤处理策略
    负责“怎么做”，包括顺序/并行关系、步骤策略、人员/设备约束。
4. 生产过程物料绑定、工序执行与追溯
    负责“现场发生了什么”，包括过站、批次、良/不良数量、执行轨迹。


这四个子域成立，不是因为“恰好四个”，而是因为它们满足两个工程条件：

1. 完备性：原始需求中的关键能力都有明确归属，没有能力空白。
2. 正交性：核心状态不被多个子域重复主写，避免职责重叠和状态争夺。

进一步说，这四个子域各自回答了不同问题：
工单域回答“当前单据处于什么状态、绑定了什么快照”；
物料/BOM 域回答“生产对象与用料结构是什么”；
工艺域回答“应该怎么做、按什么顺序或并行规则做”；
执行追溯域回答“现场到底发生了什么”。

当这四类问题被分开，系统就具备了 Agent 最需要的特性：
输入清晰、责任唯一、修改局部化、验证路径短。
这四个域是完备的，因为从“定义 -> 绑定 -> 执行 -> 追溯”闭环全覆盖。
这四个域是正交的，因为核心可写状态各有唯一责任方，不争写、不重写。


———

拆分子域对应原需求说明

## 一、先把你的原文需求换成四个业务问题

一、工单要从建立到完工全程可控，并且要记住它绑定了哪一个物料清单版本和哪一套工艺路线。  
二、物料清单本身要能维护和发版，版本发布后要可追溯、可复用。  
三、工艺路线要能定义到工序级别，每个工序要有自己的处理策略，并能约束由谁或哪台设备执行。  
四、生产现场要真实记录每一步执行过程，包括物料批次绑定、过站、良品与不良品数量、工单追溯。

## 二、四个子域分别负责什么

### 一、工单生命周期与物料清单及工艺绑定快照

这个子域只负责工单本身。  
它回答的是：工单现在处于什么阶段，绑定了哪个物料清单版本，绑定了哪套工艺路线，绑定是在什么时间由谁确认的。  
它不负责维护物料清单内容，也不负责现场过站数据录入。

对应原文：  
工单生产过程需要绑定信息。  
工单可以绑定工艺流程和工序信息。  
工单生产要受工艺约束。

### 二、物料主数据、物料清单与版本发布

这个子域只负责“做什么料、用什么料、版本是什么”。  
它维护物料基础信息、物料清单结构、版本发布与生效关系。  
它不负责工单状态推进，也不负责现场执行记录。

对应原文：  
物料有对应物料清单和版本。  
物料清单里定义当前生产物料需要的全部原料。

### 三、工艺路线、工序步骤、步骤处理策略

这个子域只负责“怎么做”。  
它维护工艺路线、工序步骤、步骤顺序或并行关系、每一步的处理策略。  
人员或设备编码属于执行约束的一部分，也在这里定义规则。  
它不负责工单推进，不负责现场数量上报。

对应原文：  
工艺流程对应多个工序步骤。  
每个步骤有自己的处理方式。  
步骤可以绑定人员或者设备编码。  
需要考虑顺序或并行执行。

### 四、生产过程物料绑定、工序执行与追溯

这个子域只负责“现场发生了什么”。  
它记录工单在每道工序上的过站、物料批次绑定、投入产出数量、良品与不良品数量。  
它负责把执行事实沉淀成可追溯记录。  
它不定义物料清单结构，也不定义工艺版本内容。

对应原文：  
工单生产过程中要做物料绑定。  
每个工艺步骤都要有批次数量、良品和不良品追踪。  
每个工单每个步骤都要记录过站信息，方便追踪。

## 三、为什么这四个子域是完备的

完备的意思是：你的原文里每个关键能力都有人负责，没有空白。

一、追踪工单：由第一子域负责状态与绑定快照，由第四子域补充现场执行轨迹。  
二、追踪物料：由第二子域定义用料规则，由第四子域记录实际批次消耗。  
三、追踪工艺：由第三子域定义路线与步骤，由第四子域记录每步执行结果。  
四、工单中途绑定：由第一子域管理绑定关系，由第四子域记录运行中绑定事实。  
五、顺序与并行：由第三子域定义关系，由第四子域按关系执行并留痕。  
六、批次与良不良：由第四子域统一记录并提供追溯查询。

所以你的需求主链路从“定义”到“绑定”到“执行”到“追溯”全部闭环，没有遗漏。

## 四、为什么这四个子域是正交的

正交的意思是：职责清楚，不互相重叠写同一类核心数据。

一、第一子域只写工单和绑定快照。  
二、第二子域只写物料与物料清单版本。  
三、第三子域只写工艺路线与步骤策略。  
四、第四子域只写现场执行与追溯事实。
这样分开后，每类核心状态只有一个主责任方，不会出现两个子域争夺同一状态写入权。  
子域之间通过约定好的消息或接口协作，而不是共享一份可随意改写的内部数据。

## 五、四个子域是不是硬编码

不是硬编码。  
这四个子域是从你的原文能力点逐条归并后得到的结果。  
如果你新增“排班计划”“设备维护”“质量放行审批”等新能力，子域数量和边界会跟着变化，不会固定永远是四个。


———

五、

先把 PRD 写成可证明的领域树，再把领域树编译成可执行的叶子任务。
Agent 不是在“读文档”，而是在“编译架构”；
没有 PRD 架构，就没有稳定的软件生成能力。
将文档微服务化：从总文档到执行文档
讲清楚目标、能力闭包、领域树、跨域契约和验收口径。它决定“做什么、谁负责”。这就是“文档即微服务组合”：主干是领域树

这也是 Harness Engineering 在 Agent 开发中的核心价值：
不是把 Agent 当“更快的程序员”，而是把交付过程重构为“可编排的认知流水线”。

从总体文档到微服务组合文档，可以落成四层：

1. 总体 PRD：定义业务目标、范围、约束与成功标准。
2. 子域规格：定义每个域的职责边界、主数据、事件与接口。
3. 原子任务卡：每张卡只做一件事，具备可验证完成条件。
4. 执行证据流：测试结果、变更记录、回归结论形成信任闭环。

当文档做到这一层，Agent 的生成行为就不再依赖“猜测上下文”，而是沿着结构化边界稳定推进。
这时你得到的，不只是“能跑的代码”，而是“可持续迭代的系统”。

———

最后：

子域数量从来不是硬编码。
它是从业务能力点“归并-去重-定责”后自然得到的结果。
新增排班、设备维护、质量放行审批，领域树会扩展；
能力消失或合并，领域树会收缩。

因此，PRD 架构不是文档格式规范，而是面向 Agent 的软件工程方法。
它让“想法”可以被证明地转化为“任务”，让“任务”可以被验证地转化为“系统”。




